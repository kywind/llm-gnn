# LLM-GNN

```
## installation
conda create -n llm-gnn python=3.9
conda activate llm-gnn
pip install -r requirements.txt

## run
# store openai API key in files/api_key.txt
# language model programs (LMPs) will be stored in lmp_output/
python run.py
```